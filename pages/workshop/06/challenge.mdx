import { MarkdownWrapper } from '../../../workshop/components/markdown-wrapper'
import { ColorSpread } from '../../../workshop/components/color-spread'

export default function ({ children }) {
  return <MarkdownWrapper>{children}</MarkdownWrapper>
}

# Reading theme values

// TODO

Instead of reaching from colors via the Tailwind config's `theme()` function, let's import the values from the `themes.js` object.

This will be a first step towards receiving dynamic values into our multi-theme plugin.

Good luck!
