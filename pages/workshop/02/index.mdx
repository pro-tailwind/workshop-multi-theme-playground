import { MarkdownWrapper } from '../../../workshop/components/markdown-wrapper'
import { ChallengeLink } from '../../../workshop/components/challenge-link'

export default function ({ children }) {
  return <MarkdownWrapper>{children}</MarkdownWrapper>
}

# Rinse and repeat

All right! Now that we've got our "proof of concept" working on the `500` color shade, let's repeat the process for all our color shades.

<ChallengeLink href="/workshop/02/challenge" text="Start challenge" />
